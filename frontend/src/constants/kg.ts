/**
 * Frontend mirror of okto_labs_pulse_core/src/okto_pulse/core/kg/schema.py
 * Kept in sync manually — when the backend schema version bumps, this
 * constant must be updated in the same PR. Drift detection is out of
 * scope for v1; the footer display is advisory only.
 */
export const SCHEMA_VERSION = '0.3.0';
